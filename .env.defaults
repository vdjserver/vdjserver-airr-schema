# AK Environment

# These are environment variables defined inside the docker containers
# This is shared across the API services

# Postgresql connection
POSTGRES_HOST=vdj-db
POSTGRES_PORT=5432
POSTGRES_DB=airr_v1
POSTGRES_USER=
POSTGRES_PASSWORD=

# where import folder is located on host machine
IMPORT_DATA=
